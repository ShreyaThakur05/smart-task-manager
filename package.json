{
  "name": "smart-task-management",
  "version": "1.0.0",
  "description": "AI-powered task management platform inspired by Trello",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd client && npm start"
  },
  "workspaces": ["client"],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}